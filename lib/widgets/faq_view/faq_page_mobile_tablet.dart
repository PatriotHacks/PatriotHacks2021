import 'package:flutter/cupertino.dart';

class FAQMobileTablet extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return ListView(
      children: [

        Column(
          crossAxisAlignment: CrossAxisAlignment.center,
        )

      ],
    );

  }



}